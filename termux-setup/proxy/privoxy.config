# Privoxy listens here; this is what Android global proxy should point to:
listen-address  127.0.0.1:9050

toggle 1
enable-remote-toggle 0
enable-remote-http-toggle 0
enable-edit-actions 0
enforce-blocks 0
debug 0

# Make this config self-contained inside your fixed folder:
confdir $HOME/.iiab-android/proxy
logdir  $HOME/.iiab-android/logs

# IMPORTANT: load only our user.action to avoid “default.action” filtering.
actionsfile $HOME/.iiab-android/proxy/user.action
